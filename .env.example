# Optional: use a stable value across deployments. When omitted, the build
# generates a private random signing key that is bundled into server code only.
VERIFICATION_SECRET=

# Used to generate absolute Open Graph URLs.
NEXT_PUBLIC_SITE_ORIGIN=https://vval.vercel.app
